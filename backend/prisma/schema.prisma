generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id       String  @id @default(cuid())
  email    String  @unique
  password String
  tasks    Task[]  // ← add this to define the opposite relation
}

model Task {
  id       String  @id @default(cuid())
  name     String
  desc     String
  date     String
  time     String
  userId   String
  user     User    @relation(fields: [userId], references: [id])
}
